const initialState = {
  isAuthenticated: null,
  user: null,
  token: null,
  isLoading: null,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
